
To use WbWbXAnalysis classes interactively (with some limits), load them when starting root in the following way:

Add the file rootlogon.C located here to your ~/.rootrc. Edit the line:

Rint.Logon:              $(CMSSW_BASE)/src/WbWbXAnalysis/Tools/root/rootlogon.C